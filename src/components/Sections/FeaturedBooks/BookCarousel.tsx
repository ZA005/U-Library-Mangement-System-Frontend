import { Box, Typography } from "@mui/material";

const BookCarousel: React.FC = () => {
    return (
        <Box>
            {/* Replace this with a carousel component later */}
            <Typography>Carousel - Minimum of 5 Books, thumbnail only</Typography>
        </Box>
    );
};

export default BookCarousel;
